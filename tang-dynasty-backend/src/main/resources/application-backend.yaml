spring:
  application:
    name: tang-dynasty-backend
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
  datasource:
    url: jdbc:mysql://localhost:3306/tang_dynasty?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: ${LOCAL_MYSQL_PWD}
    driver-class-name: com.mysql.cj.jdbc.Driver

#  flyway:
#    enabled: true
#    baseline-on-migrate: true
#    locations: classpath:db/migration

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

security:
  jwt:
    secret: ${JWT_SECRET:TangDynastyJwtSecretKeyForConsoleLoginAndAgentAccess2026}
    expiration-ms: 86400000

